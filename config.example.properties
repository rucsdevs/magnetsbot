# Copy to config.properties for local development (never commit config.properties).
# In production these are provided as environment variables instead.

BOT_TOKEN=your-bot-token-here

# Optional: where the SQLite database is stored (defaults to ./star.db)
# STAR_DB=/data/star.db
